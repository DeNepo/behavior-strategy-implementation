'use strict';

describe('primitives have a type and a value', () => {
  describe('null', () => {
    const primitive = null;
    it('type', () => {
      expect(typeof primitive).toEqual(_);
    });
    it('value', () => {
      expect(primitive).toEqual(_);
    });
  });
  describe('undefined', () => {
    const primitive = undefined;
    it('type', () => {
      expect(typeof primitive).toEqual(_);
    });
    it('value', () => {
      expect(primitive).toEqual(_);
    });
  });
  describe('Infinity', () => {
    const primitive = Infinity;
    it('type', () => {
      expect(typeof primitive).toEqual(_);
    });
    it('value', () => {
      expect(primitive).toEqual(_);
    });
  });
  describe('4', () => {
    const primitive = 4;
    it('type', () => {
      expect(typeof primitive).toEqual(_);
    });
    it('value', () => {
      expect(primitive).toEqual(_);
    });
  });
  describe('"4"', () => {
    const primitive = '4';
    it('type', () => {
      expect(typeof primitive).toEqual(_);
    });
    it('value', () => {
      expect(primitive).toEqual(_);
    });
  });
  describe('false', () => {
    const primitive = false;
    it('type', () => {
      expect(typeof primitive).toEqual(_);
    });
    it('value', () => {
      expect(primitive).toEqual(_);
    });
  });
  describe('NaN', () => {
    const primitive = NaN;
    it('type', () => {
      expect(typeof primitive).toEqual(_);
    });
    it('value', () => {
      expect(primitive).toEqual(_);
    });
  });
});
