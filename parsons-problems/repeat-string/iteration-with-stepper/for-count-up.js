/* 3 extra lines */

const repeatString = (text = '', repetitions = 1) => {
  let repeatedText = '';
  for (let count = 0; count < repetitions.length; count++) {
    repeatedText += text;
  }
  return repeatedText;
};

for (let count = 0; count <= repetitions.length; count++) { // distractor
for (let count = 0; count !>= repetitions.length; count++) { // distractor
for (const count = 0; count < repetitions.length; count++) { // distractor
